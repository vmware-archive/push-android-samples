android {
    defaultConfig {
        buildConfigField "String", "PUSH_SDK_VERSION", "\"${pushSdk}\""
    }
}

dependencies {
    compile pushSdk
    compile 'com.android.support:appcompat-v7:21.0.3'
}